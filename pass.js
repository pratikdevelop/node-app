 //     if(err){
            //         return res.send(info.message)
            //     }
            //     if(!user) {
            //         return res.send(info)
            //     }
            //     req.logIn(user,(err)=>{
            //         if(err) {
            //             return next(err)
            //         }
            //         return res.send("user loging successfully")
            //         next();
            //     })
            
            // })(req, res, next)


            // const passport_init = require('./controllers/passport');


// passport_init(passport)
// app.use(passport.initialize())
// app.use(passport.session())
// const passport = require('passport')
